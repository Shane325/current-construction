<div id="gotoTop" class="icon-angle-up"></div>
<% js.forEach(function(src){ %>
    <script src="<%=src %>"></script>
    <% }) %>
 <script>
  $(document).ready(function () {
  // bind 'myForm' and provide a simple callback function
  $('#current-contact-form').ajaxForm(function (responseText) {
    if (responseText === 'ok') {
      window.location.replace('http://www.currentcon.com/confirm')
    }
  })

  $('#estimateForm').ajaxForm(function (responseText) {
    if (responseText === 'ok') {
      window.location.replace('http://www.currentcon.com/confirm')
    }
  })
})
 </script>
